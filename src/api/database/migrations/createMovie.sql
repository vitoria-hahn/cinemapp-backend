CREATE TABLE IF NOT EXISTS movies(id UUID PRIMARY KEY, title VARCHAR NOT NULL, year INTEGER NOT NULL, genre VARCHAR[] NOT NULL, director VARCHAR NOT NULL, minutes INTEGER NOT NULL, imdbScore DECIMAL NOT NULL, summary VARCHAR NOT NULL);